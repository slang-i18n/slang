name: example
description: Example project using slang.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  slang: ^3.16.0 # add this
  slang_flutter: ^3.16.0 # add this
  flutter_localizations: # add this
    sdk: flutter
  intl: any

dev_dependencies:
  lints: ^2.0.0
#  build_runner: any
#  slang_build_runner: any

flutter:
  uses-material-design: true

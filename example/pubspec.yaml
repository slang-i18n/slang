name: example
description: Example project using fast_i18n.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fast_i18n: ^3.0.2 # add this
  cupertino_icons: ^1.0.2

dev_dependencies:
  build_runner: any # and this

flutter:
  uses-material-design: true